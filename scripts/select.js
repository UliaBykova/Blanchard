const element = document.querySelector('.js-choice');
const choices = new Choices(element, {
    searchEnabled: false,
    itemSelectText: '',
    shouldSort: false,
    placeholder: false,
});

const elements = document.querySelectorAll('.js-choice');

const choices1 = new Choices(elements[1], {
    searchEnabled: false,
    itemSelectText: '',
    shouldSort: false,
    placeholder: false,
});

const choices2 = new Choices(elements[2], {
    searchEnabled: false,
    itemSelectText: '',
    shouldSort: false,
    placeholder: false,
});

const choices3 = new Choices(elements[3], {
    searchEnabled: false,
    itemSelectText: '',
    shouldSort: false,
    placeholder: false,
});

const choices4 = new Choices(elements[4], {
    searchEnabled: false,
    itemSelectText: '',
    shouldSort: false,
    placeholder: false,
});





